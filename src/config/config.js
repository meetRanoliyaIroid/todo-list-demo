import { DB_USERNAME, DB_PASSWORD, DB_NAME, DB_HOST, DB_PORT, DB_CONNECTION } from "./constant.js";

export default {
  "username": DB_USERNAME,
  "password": DB_PASSWORD,
  "database": DB_NAME,
  "host": DB_HOST,
  "port": DB_PORT,
  "dialect": DB_CONNECTION,
  "logging": false
};

